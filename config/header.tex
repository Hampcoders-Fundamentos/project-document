\usepackage{graphicx}
\usepackage{float}
\usepackage{seqsplit}
\usepackage{needspace}
\usepackage{enumitem}

% Soporte de color, fuentes avanzadas y herramientas de entorno
\usepackage{xcolor}
\usepackage{fontspec}
\usepackage{etoolbox}

\usepackage{fvextra}
\usepackage{makecell}

% Configuración para evitar que los guiones bajos en los nombres de archivos se rompan en varias líneas
\usepackage[strings]{underscore}

\makeatletter
\def\fps@figure{H}
\def\fps@table{H}
\makeatother

\setkeys{Gin}{width=\linewidth,height=0.45\textheight,keepaspectratio}
\usepackage{longtable}
\usepackage{array}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multirow}

\RecustomVerbatimEnvironment{verbatim}{Verbatim}{breaklines,breakanywhere}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines,breakanywhere,commandchars=\\\{\}}
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.5}

% Configuración de fuentes en entornos tabulares
\AtBeginEnvironment{tabular}{\footnotesize}
\AtBeginEnvironment{tabularx}{\footnotesize}
\AtBeginEnvironment{longtable}{\footnotesize}

% Desactiva ligaduras comunes (fi, fl, ff) que fragmentan caracteres durante la extracción
\defaultfontfeatures{Ligatures=NoCommon}

% Configuración compatible con Tectonic local usando nombres de archivo tipográficos estrictos
\setmainfont{lmroman10-regular.otf}[
  BoldFont = lmroman10-bold.otf,
  ItalicFont = lmroman10-italic.otf,
  BoldItalicFont = lmroman10-bolditalic.otf,
  WordSpace={1,1,1}
]

% Desactivación de protrusión para evitar compresión de palabras en entornos de Pandoc
\XeTeXprotrudechars=0
\XeTeXlinebreaklocale "es"

% Habilita mapeo ActualText por glifo para extracción correcta de texto
\ifdefined\XeTeXgenerateactualtext
  \XeTeXgenerateactualtext=1
\fi

\usepackage{newunicodechar}
\definecolor{emojiyellow}{HTML}{FFD700}
\newfontfamily{\emojifont}{Segoe UI Emoji}[Renderer=HarfBuzz,Color=FFD700]
\newunicodechar{⭐}{{\emojifont\textcolor{emojiyellow}{⭐}}}

% Soporte nativo a Gherkin (Cucumber)
\usepackage{listings}

% Definición de la paleta de colores para el código Gherkin
\definecolor{gherkinKeyword}{HTML}{000080}   % Azul oscuro para palabras clave
\definecolor{gherkinString}{HTML}{008000}    % Verde para cadenas de texto/variables
\definecolor{gherkinComment}{HTML}{808080}   % Gris para comentarios
\definecolor{gherkinText}{HTML}{000000}      % Negro para texto plano
\definecolor{gherkinBg}{HTML}{F8F8F8}        % Gris muy claro para el fondo

\lstdefinelanguage{Gherkin}{
  keywords={Feature, Background, Scenario, Scenario Outline, Given, When, Then, And, But, Examples, Functional Requirement, Business Need, Requirement, Scenario Template, Scenarios},
  sensitive=true,
  comment=[l]{\#},
  morestring=[b]",
  morestring=[b]'
}

\lstset{
  language=Gherkin,
  backgroundcolor=\color{gherkinBg},
  basicstyle=\small\ttfamily\color{gherkinText},
  keywordstyle=\bfseries\color{gherkinKeyword},
  stringstyle=\color{gherkinString},
  commentstyle=\itshape\color{gherkinComment},
  numbers=left,
  numberstyle=\tiny\color{gherkinComment},
  stepnumber=1,
  numbersep=8pt,
  showstringspaces=false,
  breaklines=true,
  breakatwhitespace=true,
  tabsize=2,
  frame=single,
  rulecolor=\color{gherkinComment!40},
  captionpos=b,
  keepspaces=true
}

% Forzar el centrado automático de todas las figuras del documento
\usepackage[justification=centering]{caption}

% Reemplazo seguro de entornos flotantes para evitar degradación del espacio interpalabras en la extracción
\makeatletter
\renewenvironment{figure}[1][]{%
  \gdef\@captype{figure}%
  \par\medskip
  \centering
  \leavevmode
}{%
  \par\medskip
}

\renewenvironment{table}[1][]{%
  \gdef\@captype{table}%
  \par\medskip
  \centering
  \leavevmode
}{%
  \par\medskip
}
\makeatother